export const WEB_URL = "https://github.com"

export const API_URL = "https://api.github.com"

export const USER = "facebook"

export const REPO = "react"

export const REPO_URL = `${API_URL}/repos/${USER}/${REPO}`

export const REPO_ISSUES_URL = `${REPO_URL}/issues?page=`
